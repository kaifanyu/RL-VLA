Put original data in `raw/` and converted episodes in `processed/`. Fill `manifest.example.json` when the dataset is known. See [the dataset contract](../docs/datasets.md). No data has been downloaded.


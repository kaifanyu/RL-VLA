Training creates one new directory per run with residual checkpoints, manifest, JSONL metrics and summary. Evaluation writes a separate JSON file. Bulk run contents are ignored by Git. `validation_*` directories, if present, are CPU plumbing checks rather than VLA benchmark results.

